'use strict';

let a = 2;

/* ответ: a = 4, x = 5
1. выполняется действие в скобках: значение а умножается на 2 (=4)
и данное значение записывается в переменную а
2. вычисляется 1 + 4 = 5
3. результат вычисления (5) записывается в переменную х
*/
let x = 1 + (a *= 2);